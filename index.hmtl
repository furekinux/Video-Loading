<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Black Friday - CampusLands</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            cursor: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="white" stroke="black" stroke-width="1"><path d="M3 3l7.07 16.97 2.51-7.39 7.39-2.51L3 3z"/></svg>'), auto;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #1a0033 0%, #000000 50%, #001a33 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            color: white;
            overflow: hidden;
        }

        .logo {
            margin-bottom: 80px;
            text-align: center;
        }

        .logo-circle {
            width: 80px;
            height: 80px;
            margin: 0 auto 10px;
            position: relative;
        }

        .circle-outer {
            width: 70px;
            height: 70px;
            border: 3px solid white;
            border-radius: 50%;
            position: absolute;
            top: 5px;
            left: 5px;
        }

        .circle-inner {
            width: 50px;
            height: 25px;
            border: 3px solid white;
            border-radius: 50% 50% 0 0;
            position: absolute;
            top: 25px;
            left: 15px;
            border-bottom: none;
        }

        .logo-text {
            font-size: 14px;
            font-weight: 400;
            letter-spacing: 2px;
        }

        .black-friday-text {
            font-family: 'Impact', 'Arial Black', sans-serif;
            font-size: 120px;
            font-weight: 900;
            line-height: 0.9;
            text-align: center;
            margin-bottom: 30px;
            letter-spacing: -2px;
        }

        .date-text {
            font-size: 24px;
            font-weight: 600;
            text-align: center;
            margin-bottom: 80px;
        }

        .loading-container {
            width: 380px;
            margin-bottom: 20px;
        }

        .progress-bar {
            width: 100%;
            height: 35px;
            background: transparent;
            border: 3px solid white;
            border-radius: 25px;
            padding: 3px;
            position: relative;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #4da3ff 0%, #7b5cff 100%);
            border-radius: 20px;
            width: 0%;
            transition: width 0.1s ease-out;
        }

        .loading-text {
            text-align: center;
            font-size: 20px;
            font-weight: 400;
            margin-top: 15px;
        }

        .cursor-icon {
            position: fixed;
            top: 45%;
            right: 15%;
            width: 60px;
            height: 60px;
            pointer-events: none;
            z-index: 1000;
        }

        .cursor-icon svg {
            width: 100%;
            height: 100%;
            filter: drop-shadow(2px 2px 4px rgba(0,0,0,0.5));
        }
    </style>
</head>
<body>
    <div class="logo">
        <div class="logo-circle">
            <div class="circle-outer"></div>
            <div class="circle-inner"></div>
        </div>
        <div class="logo-text">campuslands</div>
    </div>

    <div class="black-friday-text">
        BLACK<br>FRIDAY
    </div>

    <div class="date-text">
        Noviembre 28, 29 & 30
    </div>

    <div class="loading-container">
        <div class="progress-bar">
            <div class="progress-fill" id="progressFill"></div>
        </div>
        <div class="loading-text">Cargando descuentos...</div>
    </div>

    <div class="cursor-icon">
        <svg viewBox="0 0 24 24" fill="white" stroke="black" stroke-width="0.5">
            <path d="M3 3l7.07 16.97 2.51-7.39 7.39-2.51L3 3z"/>
        </svg>
    </div>

    <script>
        let progress = 0;
        const targetProgress = 85;
        const progressFill = document.getElementById('progressFill');
        
        const interval = setInterval(() => {
            if (progress < targetProgress) {
                progress += 1;
                progressFill.style.width = progress + '%';
            } else {
                clearInterval(interval);
            }
        }, 15);
    </script>
</body>
</html>
